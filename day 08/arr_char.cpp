//arr_char

#include<stdio.h>

int main() {


	/*char a = 'a'
			//하나만 넣었는데 여러 개 넣을 것임*/

	//char coffee[5] = { 'l','a','t','t','e' };

	//for (int i = 0; i < 5; i++) {
	//	printf("%c", coffee[i]);
	//}
	//printf("\n");
	//printf("%s", coffee);

	//char coffee[5] = { 'l','a','t','t','\0' };

	//for (int i = 0; i < 5; i++) {
	//	printf("%c", coffee[i]);
	//}
	//printf("\n");
	//printf("%s", coffee);


	// *** 문자 char a = 'a' ***
	// *** 문자열 char coffee[]의 끝은 무조건 null문자 ('\0')로 끝내야 함 ***
	
	/*char coffee[] = {'a','m','e','r','i','c','a','n','o','\0'};

	printf("sizeof coffee: %d\n", sizeof(coffee));

	for (int i = 0; i < 9; i++) { // 9자리에 sizeof(coffee)를 넣을 수 있다. 굳이 세지 않고 sizeof를 사용 가능
		printf("%c", coffee[i]); // sizeof가 10자리가 되니까 i < 9하는 것보다 sizeof(coffee)를 써서 넣는 게 더 나음
	}
	
	printf("\n");
	printf("%s", coffee);


	char b[5] = { 'a','b','c','d','\0' }; // %s를 쓸 거면 끝에 \0을 넣어줘야 함
	*/







	return 0;
}